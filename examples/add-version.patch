diff --git a/bin/request-report.mjs b/bin/request-report.mjs
--- a/bin/request-report.mjs
+++ b/bin/request-report.mjs
@@ -36,4 +36,8 @@
 export function main(args) {
+  if (args.length === 1 && args[0] === '--version') {
+    console.log('1.0.0')
+    return
+  }
   if (args.length === 1 && args[0] === '--help') {
     console.log('Usage: node bin/request-report.mjs <requests.jsonl> [--format text|json|html]')
     console.log('Print request count, server errors, p95 latency, and route counts.')
diff --git a/test/report.test.mjs b/test/report.test.mjs
--- a/test/report.test.mjs
+++ b/test/report.test.mjs
@@ -6,3 +6,8 @@
+test('the version command works without an input file', () => {
+  const output = execFileSync(process.execPath, ['bin/request-report.mjs', '--version'], { encoding: 'utf8' })
+  assert.equal(output.trim(), '1.0.0')
+})
+
 test('summarizes requests, server errors and nearest-rank p95', () => {
   assert.deepEqual(summarize('{"path":"/a","status":200,"durationMs":10}\n{"path":"/a","status":503,"durationMs":50}\n'), {
     requests: 2, errors: 1, p95Ms: 50, routes: { '/a': 2 },
